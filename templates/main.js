import React from 'react';
import ReactDOM from 'react-dom';
import CyberpunkAirQuality from './CyberpunkAirQuality';

ReactDOM.render(
  <React.StrictMode>
    <CyberpunkAirQuality />
  </React.StrictMode>,
  document.getElementById('root')
);